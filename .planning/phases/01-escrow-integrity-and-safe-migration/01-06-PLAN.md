---
phase: 01-escrow-integrity-and-safe-migration
plan: 06
type: execute
wave: 5
depends_on: [01-03, 01-05]
files_modified: []
autonomous: false
requirements: []
user_setup:
  - service: production-release
    why: "Live mutation requires named operator approval and a single-writer window"
    env_vars:
      - { name: ESCROW_LIVE_DB, source: "Approved PF-01 inventory" }
      - { name: ESCROW_EVIDENCE_DIR, source: "Unique signed release evidence directory" }
must_haves:
  truths:
    - "Final restored-copy rehearsal uses the actual candidate and an actual corrected-core rollback artifact before live apply."
    - "PF-06 approval binds both artifact digests; rollback to any pre-integrity artifact is forbidden."
    - "Live apply cannot begin until every PF item, HIGH disposition and hash-bound one-time authorization is signed."
    - "Production pre/post audits target two distinct immutable snapshots, never the live database."
    - "Any stop condition leaves rollout halted without down-migration or unsafe rollback."
  artifacts:
    - { path: "$env:ESCROW_EVIDENCE_DIR/final-preflight-index.json", provides: "Signed authorization inputs" }
    - { path: "$env:ESCROW_EVIDENCE_DIR/restored-copy-report.json", provides: "Actual candidate and corrected rollback rehearsal evidence" }
    - { path: "$env:ESCROW_EVIDENCE_DIR/post-audit.json", provides: "Immutable post-apply domain evidence" }
  key_links:
    - { from: "approval token", to: "production apply", via: "pre-snapshot/audit/manifest/candidate/rollback/single-writer hashes plus exact live path", pattern: "approve-production-apply" }
---
<objective>Authorize and execute production mutation only through the reviewed immutable-snapshot contract.</objective>
<execution_context>@C:/Users/dev1853/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/dev1853/.codex/get-shit-done/templates/summary.md</execution_context>
<context>
@docs/ESCROW_MIGRATION_RUNBOOK.md
@.planning/phases/01-escrow-integrity-and-safe-migration/01-VALIDATION.md
@.planning/phases/01-escrow-integrity-and-safe-migration/01-03-SUMMARY.md
@.planning/phases/01-escrow-integrity-and-safe-migration/01-04-SUMMARY.md
@.planning/phases/01-escrow-integrity-and-safe-migration/01-05-SUMMARY.md
</context>
<tasks>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Rehearse actual artifacts and sign PF-06 production authority</name>
  <files>Evidence only</files>
  <action>After 01-04 and 01-05 outputs exist, build the actual candidate artifact and a separate rollback artifact that contains the same corrected escrow core and reads the expanded nullable schema; explicitly reject any pre-integrity rollback artifact. In one PowerShell session validate `$env:ESCROW_LIVE_DB`, `$env:ESCROW_RESTORED_DB`, and `$env:ESCROW_EVIDENCE_DIR`; create the unique immutable pre snapshot, audit only that snapshot with `--live-path "$env:ESCROW_LIVE_DB"`, restore it to the distinct restored path, and run restore verification. On the restored copy only, prove schema equivalence, baseline resolution, migrate deploy, integrity/FK/counts, dry-run, reviewer dispositions, `--rehearsal-apply`, and a second rehearsal apply with `mutations=0`; boot/smoke both the actual candidate and actual corrected rollback artifacts against copies of the expanded restored DB. Capture PF-03 runtime from the candidate and finalize PF-01…PF-06. Create canonical authorization JSON containing the resolved exact live path and SHA-256 values of the pre snapshot, pre-audit, approved manifest, candidate artifact, corrected rollback artifact and single-writer evidence; derive the one-time token as SHA-256 of the versioned canonical object. Any missing/mismatch, unresolved HIGH, unsafe runtime/topology, artifact smoke failure or rollback lacking the corrected core stops without live mutation.</action>
  <verify><automated>node -e "for(const n of ['ESCROW_LIVE_DB','ESCROW_RESTORED_DB','ESCROW_EVIDENCE_DIR','ESCROW_PRE_SNAPSHOT_SHA256','ESCROW_PRE_AUDIT_SHA256','ESCROW_MANIFEST_SHA256','ESCROW_CANDIDATE_SHA256','ESCROW_ROLLBACK_SHA256','ESCROW_SINGLE_WRITER_SHA256','ESCROW_APPROVAL_TOKEN'])if(!process.env[n]?.trim())throw Error('missing '+n)"</automated></verify>
  <what-built>Signed restored-copy report, PF-06 candidate/rollback approval and one-time authorization object.</what-built>
  <how-to-verify>Review all six bound hashes, exact live path, candidate and corrected rollback smoke logs, runtime/topology/policy, every ambiguity ID, second rehearsal zero and all HIGH dispositions.</how-to-verify>
  <resume-signal>Enter `approve-production-apply &lt;authorization-sha256&gt; &lt;one-time-token&gt;`; any other response stops.</resume-signal>
  <done>Actual candidate and corrected rollback artifacts have passed restored-copy rehearsal, and PF-06 plus exact production authority are hash-bound.</done>
</task>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Apply once and audit a separate immutable post-apply snapshot</name>
  <files>Evidence only; source files unchanged</files>
  <action>Execute and record this exact fail-closed production sequence in `$env:ESCROW_EVIDENCE_DIR/production-sequence.json`: (1) stop writes and enter the approved single-writer maintenance window, then re-hash its evidence; (2) verify the non-autonomous approval token, exact resolved live path and all six bound immutable evidence hashes without consuming or reserving the token; (3) bind `DATABASE_URL` to that exact confirmed `$env:ESCROW_LIVE_DB` and run `npm run migrate:deploy` as the separate reviewed schema step, never through application startup; (4) before any backfill write, prove `Deal.escrowTransactionId` and `Transaction.businessKey` exist, migration state is current, `integrity_check` is `ok`, `foreign_key_check` returns no rows, and pre-backfill counts/domain conditions still match the approved manifest; (5) only after every schema gate passes, invoke the backfill exactly once with `--production-apply`, `--database "$env:ESCROW_LIVE_DB"`, `--live-path "$env:ESCROW_LIVE_DB"`, `--confirm-live-path "$env:ESCROW_LIVE_DB"`, all six immutable hashes, the approval token and exclusive consumption ledger, with token consumption occurring immediately before the first backfill write; never retry production apply, and require token reuse to fail closed because second-apply idempotency was proved in Task 1 rehearsal; (6) keep reads off and create a distinct immutable `$env:ESCROW_POST_SNAPSHOT`, back up live to that new target and independently verify its path and checksum; (7) audit only the post snapshot using `--live-path "$env:ESCROW_LIVE_DB"` and compare row, balance, held-credit and counter deltas to the approved manifest; (8) run corrected-core flag-off smoke, enable reads and resume writes only after every preceding gate and read smoke pass. Dry-run, audit, backup verification and restored-copy tools remain snapshot-only and must reject the resolved live path. On any missing column, migration drift, integrity/FK/domain failure, HIGH, unexplained delta, consumed/mismatched token, checksum/path failure or busy anomaly, record the failed step, keep writes stopped, do not consume the token if step 5 has not begun, perform no down-migration, and follow the corrected-core rollback procedure; DB restore remains forbidden until post-snapshot writes are reconciled.</action>
  <verify><automated>node -e "const fs=require('fs'),p=process.env.ESCROW_EVIDENCE_DIR+'/production-sequence.json',x=JSON.parse(fs.readFileSync(p,'utf8')),want=['writes-stopped','authorization-verified-unconsumed','migrate-deploy','schema-and-domain-gates','production-apply-once','post-snapshot','post-audit-and-deltas','writes-resumed'];if(JSON.stringify(x.steps?.map(function(s){return s.id}))!==JSON.stringify(want)||x.applyAttempts!==1||x.tokenConsumedAtStep!=='production-apply-once'||!x.steps.every(function(s){return s.status==='passed'}))throw Error('invalid production order/evidence')"</automated></verify>
  <what-built>Production migration evidence with distinct immutable pre/post audits.</what-built>
  <how-to-verify>Confirm `production-sequence.json` records, in order: writes stopped; token and six hashes verified while unused; migrate deploy against the confirmed live path; schema/migration/integrity/FK/pre-backfill domain gates; exactly one production apply with token consumption immediately before its first write; distinct immutable post snapshot; post-snapshot audit with row/balance/hold/counter deltas matching the manifest; and writes resumed only after all gates. On failure, confirm writes remain stopped and the corrected-core rollback procedure is followed.</how-to-verify>
  <resume-signal>Enter `production-evidence-approved &lt;post-snapshot-sha256&gt; &lt;post-audit-sha256&gt;` or report the stop condition.</resume-signal>
  <done>Production is either migrated with complete evidence or safely halted without unsafe rollback.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-12 | Tampering / Repudiation | live apply | mitigate + blocking gate | Exact approval token, hashes, single writer and post-audit. |
| T-01-13 | Disclosure / Availability | snapshots | mitigate + blocking gate | Approved policy, distinct immutable targets and checksums. |
| T-01-14 | Availability | restore | mitigate | Stop writes plus reconciliation; otherwise forward repair. |
| T-01-15 | Elevation | unresolved HIGH | mitigate | Any unresolved HIGH blocks resume. |
</threat_model>
<verification>Before Task 1 approval, run the phase gate `node --test --test-concurrency=1 test/*.test.mjs` and `npm run build`. Task verifies remain focused under 30 seconds. All PowerShell references use `$env:NAME`; non-mutating tools target immutable snapshots, and production apply fails closed unless exact live path, six hashes and the unused one-time token agree.</verification>
<success_criteria>Production apply has explicit authority, immutable pre/post evidence and no path back to unsafe escrow logic.</success_criteria>
<output>After completion create `.planning/phases/01-escrow-integrity-and-safe-migration/01-06-SUMMARY.md`.</output>
