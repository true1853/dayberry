---
phase: 01-escrow-integrity-and-safe-migration
plan: 05
type: execute
wave: 4
depends_on: [01-04]
files_modified: [lib/deals/rollout.js, src/server/actions.js, Dockerfile, docker-compose.yml, package.json, test/compatibility.integration.test.mjs, docs/ESCROW_MIGRATION_RUNBOOK.md]
autonomous: true
requirements: [SAFE-03]
must_haves:
  truths:
    - "Expanded reads can be disabled without disabling chat or corrected direct escrow commands."
    - "Application has exactly one Docker CMD/ENTRYPOINT and it equals the approved direct Next.js production startup command."
    - "Runbook uses separate immutable pre/post snapshots for all production domain audits."
  artifacts:
    - { path: lib/deals/rollout.js, provides: "Server-only read projection flag" }
    - { path: docs/ESCROW_MIGRATION_RUNBOOK.md, provides: "Exact PowerShell release/stop/rollback protocol" }
    - { path: test/compatibility.integration.test.mjs, provides: "Flag-off/on corrected-core proof" }
  key_links:
    - { from: src/server/actions.js, to: lib/deals/rollout.js, via: "read projection only", pattern: "expandedDealReadInclude|serializeEscrowReadState" }
    - { from: Dockerfile, to: package.json, via: "migration commands excluded from startup", pattern: "CMD|ENTRYPOINT" }
---
<objective>Add reversible read compatibility, non-mutating startup and an executable immutable-snapshot runbook.</objective>
<execution_context>@C:/Users/dev1853/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/dev1853/.codex/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/01-escrow-integrity-and-safe-migration/01-RESEARCH.md
@.planning/phases/01-escrow-integrity-and-safe-migration/01-VALIDATION.md
@.planning/phases/01-escrow-integrity-and-safe-migration/01-04-SUMMARY.md
@Dockerfile
@docker-compose.yml
@package.json
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Gate reads and make application startup non-mutating</name>
  <files>lib/deals/rollout.js, src/server/actions.js, Dockerfile, docker-compose.yml, package.json, test/compatibility.integration.test.mjs</files>
  <behavior>
    - Only normalized `1` enables optional non-sensitive `escrowLinkState`; raw transaction IDs never serialize.
    - Corrected core runs identically flag off/on.
    - Docker has no ENTRYPOINT and its sole CMD exactly equals `CMD ["node", "node_modules/next/dist/bin/next", "start", "-p", "80"]`.
  </behavior>
  <action>Test flag parsing, payload compatibility and corrected create/complete/cancel/dispute with flag off/on. Add pure read helpers and keep the flag out of `lib/deals/escrow.js`. Add separate `migrate:deploy`, dry-run/rehearsal/production backfill and compatibility scripts. Replace the current shell-chain startup with exactly `CMD ["node", "node_modules/next/dist/bin/next", "start", "-p", "80"]`; allow no ENTRYPOINT and do not invoke an npm alias at runtime. Add an exact allowlist assertion requiring that single literal startup instruction. Because this is an allowlist, any wrapper, shell, npm alias, migration, `migrate-chains`, `migrate-photos`, restore, backup, audit, backfill, Prisma maintenance command (`db push`, `migrate deploy/dev/reset`) or later-added script fails automatically instead of depending on a denylist.</action>
  <verify>
    <automated>node --test test/compatibility.integration.test.mjs; node -e "const fs=require('fs');const x=fs.readFileSync('Dockerfile','utf8').split(/\r?\n/).map(s=>s.trim()).filter(s=>/^(CMD|ENTRYPOINT)\b/i.test(s));const approved='CMD [\"node\", \"node_modules/next/dist/bin/next\", \"start\", \"-p\", \"80\"]';if(x.length!==1||x[0]!==approved)throw Error('startup is not exact allowlist: '+JSON.stringify(x))"</automated>
  </verify>
  <done>Read rollout is reversible while startup and corrected financial integrity are not flag-controlled.</done>
</task>
<task type="auto">
  <name>Task 2: Write exact PowerShell immutable-snapshot release and rollback runbook</name>
  <files>docs/ESCROW_MIGRATION_RUNBOOK.md</files>
  <action>Document PF-01…PF-07 owners, inputs, outputs, pass/stop criteria and exact PowerShell. Require `$env:ESCROW_LIVE_DB`, `$env:ESCROW_EVIDENCE_DIR`, `$env:ESCROW_RESTORED_DB`; throw on blanks. Pre-audit sequence creates `$env:ESCROW_PRE_SNAPSHOT` via `escrow:backup`, audits only that snapshot, and rehearses `--rehearsal-apply` twice on the restored copy. PF-06 is approved only in 01-06 after the actual candidate and a corrected-core/expanded-schema-compatible rollback artifact both pass rehearsal; pre-integrity rollback is forbidden. Define the canonical authorization object and one-time token bound to pre-snapshot, pre-audit, manifest, candidate, corrected rollback, single-writer hashes and exact live path. Production uses only `--production-apply` with `--database`, `--live-path`, and `--confirm-live-path` all resolving to the approved live path plus the exclusive consumption ledger; plain `--apply` is forbidden. After production apply, create and independently verify a distinct `$env:ESCROW_POST_SNAPSHOT`, then audit it; never audit live directly. Include schema equivalence, baseline resolve, migrate deploy, integrity/FK/count audit, ambiguity review, flag smoke and evidence index. Database restore requires stop-writes and reconciliation. Forbid guessed paths, `prisma/dev.db`, db push and chain completion changes.</action>
  <verify><automated>node -e "const fs=require('fs');const s=fs.readFileSync('docs/ESCROW_MIGRATION_RUNBOOK.md','utf8');for(const x of ['$env:ESCROW_LIVE_DB','$env:ESCROW_PRE_SNAPSHOT','$env:ESCROW_POST_SNAPSHOT','pre-apply-snapshot','post-apply-snapshot','migrate:deploy','approve-production-apply','reconciliation'])if(!s.includes(x))throw Error('missing '+x)"</automated></verify>
  <done>Runbook makes the safe production contract directly executable in PowerShell and fails closed at every unknown/HIGH condition.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-10 | Tampering | feature rollback | mitigate | Flag touches reads only; corrected-core tests in both modes. |
| T-01-11 | Tampering / Denial | startup | mitigate | Exact one-line CMD allowlist permits only direct Next.js production startup; wrappers, aliases and every maintenance script fail the assertion. |
| T-01-14 | Availability | rollback | mitigate | Corrected artifact rollback; DB restore only after reconciliation. |
</threat_model>
<verification>Run only the focused commands above per task. At the Wave 4 gate run `node --test --test-concurrency=1 test/*.test.mjs` and `npm run build`; runbook has exact snapshot-only commands.</verification>
<success_criteria>SAFE-03 appears only here; read rollout is reversible and startup cannot mutate schema/finance.</success_criteria>
<output>After completion create `.planning/phases/01-escrow-integrity-and-safe-migration/01-05-SUMMARY.md`.</output>
