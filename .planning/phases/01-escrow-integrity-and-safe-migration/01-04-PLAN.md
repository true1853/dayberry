---
phase: 01-escrow-integrity-and-safe-migration
plan: 04
type: execute
wave: 3
depends_on: [01-02]
files_modified: [lib/deals/escrow.js, src/server/actions.js, src/App.jsx, test/escrow.integration.test.mjs, test/escrow-concurrency.integration.test.mjs]
autonomous: true
requirements: [SAFE-01]
must_haves:
  truths:
    - "Every positive-credit direct deal owns exactly one exact linked hold; zero-credit deals own none."
    - "Create, confirm, cancel, dispute and resolve are atomic and at-most-once under replay/race."
    - "Direct code never selects a chain hold; notifications run only after commit."
  artifacts:
    - { path: lib/deals/escrow.js, provides: "Exact injectable direct-deal commands" }
    - { path: test/escrow-concurrency.integration.test.mjs, provides: "Barriered at-most-once proof" }
  key_links:
    - { from: src/server/actions.js, to: lib/deals/escrow.js, via: "all direct/moderation entry paths", pattern: "createDealWithEscrow|confirmDealSide|cancelDealWithEscrow|openDealDispute|resolveDealDispute" }
---
<objective>Replace every unsafe direct-deal financial path with one exact atomic escrow core.</objective>
<execution_context>@C:/Users/dev1853/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/dev1853/.codex/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/01-escrow-integrity-and-safe-migration/01-RESEARCH.md
@.planning/phases/01-escrow-integrity-and-safe-migration/01-VALIDATION.md
@.planning/phases/01-escrow-integrity-and-safe-migration/01-02-SUMMARY.md
@prisma/schema.prisma
@lib/deals/escrow-invariants.js
@src/server/actions.js
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement exact direct-deal commands and rollback invariants</name>
  <files>lib/deals/escrow.js, test/escrow.integration.test.mjs</files>
  <behavior>
    - Positive create atomically debits, creates Deal, exact `refType=deal` hold and FK/business key; zero credit creates no hold.
    - Stable create retry returns the same economic result; changed payload conflicts.
    - Terminal commands claim legal Deal state and exact hold predicate in one short transaction.
    - Any mismatch/failpoint rolls back Deal, Transaction, balances, counters and earnings.
  </behavior>
  <action>Write failing sequential/failpoint tests first. Implement injectable commands and typed errors. Exact hold predicates include transaction ID, initiator, kind, amount, held status, `refType='deal'` and `refId=deal.id`. Explicitly reject `refType='chain'`; chain completion is outside Phase 1. Use conditional claims and stable unique keys; do no external I/O in transactions and no user/time/latest-held query.</action>
  <verify><automated>node --test --test-name-pattern="zero-credit|exact|rollback|chain" test/escrow.integration.test.mjs</automated></verify>
  <done>Exact invariant failures produce zero partial writes and no chain hold can enter a direct operation.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Route all entry paths and prove race safety</name>
  <files>src/server/actions.js, src/App.jsx, test/escrow-concurrency.integration.test.mjs</files>
  <behavior>
    - Duplicate create, complete/complete, cancel/complete, dispute/terminal and duplicate admin resolution have one winner/effect.
    - Actor derives from authenticated session; moderation uses the same domain command.
    - Client command ID remains stable only for the same pending economic payload.
  </behavior>
  <action>Build barriered two-client tests plus reconnect retry. Route create, both confirmations, cancel, participant/moderation dispute and both admin outcomes through the core; remove legacy completeDeal and every direct latest-held lookup. Map known errors at controller boundary and rethrow unknown ones. Send notifications only after successful command return and only for the CAS winner. Add stable `clientCommandId` behavior in App.</action>
  <verify><automated>node --test --test-name-pattern="duplicate create|cancel vs complete" test/escrow-concurrency.integration.test.mjs</automated></verify>
  <done>All direct entry paths are exact, atomic and at-most-once under retry/race.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-05 | Tampering / Elevation | terminal lookup | mitigate | Exact FK/full predicate, auth guards, source gate banning latest-held. |
| T-01-06 | Repudiation | replay/race | mitigate | CAS, unique keys and two-client tests. |
| T-01-07 | Tampering | split commit | mitigate | One short transaction and failpoint rollback tests. |
| T-01-08 | Elevation | moderation | mitigate | Admin guard and shared commands. |
| T-01-09 | Denial | SQLITE_BUSY | mitigate | Short transactions; retry only with same proven idempotency key. |
</threat_model>
<verification>Run the focused commands above per task. At the Wave 3 gate, run `node --test --test-concurrency=1 test/escrow.integration.test.mjs test/escrow-concurrency.integration.test.mjs`; source has no direct latest-held lookup.</verification>
<success_criteria>SAFE-01 appears only here; every direct financial effect is exact and at-most-once.</success_criteria>
<output>After completion create `.planning/phases/01-escrow-integrity-and-safe-migration/01-04-SUMMARY.md`.</output>
