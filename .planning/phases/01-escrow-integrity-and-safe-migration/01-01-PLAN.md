---
phase: 01-escrow-integrity-and-safe-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/deals/escrow-invariants.js
  - scripts/audit-deal-escrow.mjs
  - scripts/backup-snapshot.mjs
  - scripts/verify-restored-copy.mjs
  - test/migration.integration.test.mjs
  - package.json
autonomous: true
requirements: []
must_haves:
  truths:
    - "Audit never opens the resolved live database and produces deterministic row-ID buckets from an immutable snapshot."
    - "Backup creates a unique consistent immutable snapshot with checksum, runtime, PRAGMA, integrity and FK evidence."
    - "Direct-deal classification never consumes, relabels or settles a refType=chain hold."
  artifacts:
    - path: scripts/audit-deal-escrow.mjs
      provides: read-only snapshot-only domain audit
    - path: scripts/backup-snapshot.mjs
      provides: consistent immutable snapshot and evidence manifest
    - path: scripts/verify-restored-copy.mjs
      provides: isolated restore proof
    - path: lib/deals/escrow-invariants.js
      provides: deterministic direct-deal candidate classifier
  key_links:
    - from: scripts/audit-deal-escrow.mjs
      to: lib/deals/escrow-invariants.js
      via: one canonical classification object
      pattern: "classifyEscrowCandidateGraph"
    - from: scripts/verify-restored-copy.mjs
      to: scripts/audit-deal-escrow.mjs
      via: snapshot-only domain audit
      pattern: "mode=ro|live-path"
---

<objective>
Build the non-mutating evidence tools used by every later migration gate.

Purpose: production pre/post audits must be executable without granting audit code write access to the live database.
Output: deterministic invariants, immutable snapshot/audit/restore CLIs, focused tests and package commands.
</objective>

<execution_context>
@C:/Users/dev1853/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/dev1853/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-escrow-integrity-and-safe-migration/01-RESEARCH.md
@.planning/phases/01-escrow-integrity-and-safe-migration/01-VALIDATION.md
@.planning/phases/01-escrow-integrity-and-safe-migration/01-PATTERNS.md
@prisma/schema.prisma
@lib/prisma.js
@scripts/backup-snapshot.mjs
@scripts/migrate-chains.mjs
@package.json

<interfaces>
Audit CLI requires `--database`, `--live-path`, and `--output`. Backup requires `--source`, `--target`, and `--evidence`. Restore verification requires `--database`, `--live-path`, and `--evidence-dir`. There are no default database paths.

Every CLI resolves absolute paths, rejects absent/blank CLI values and absent environment fallbacks, and exits nonzero before opening a database if required input is missing. `audit` and `verify-restored-copy` reject resolved equality with the live path and open only `file:...?...mode=ro`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define deterministic direct-deal audit invariants</name>
  <files>lib/deals/escrow-invariants.js, test/migration.integration.test.mjs</files>
  <behavior>
    - Stable input rows produce stable sorted buckets and hashes.
    - Automatic pairs require globally unique 1:1 user/amount/kind/status/ref agreement.
    - Ambiguous, corrupt, dangling and zero-credit violations retain exact row IDs and never become writes.
    - Chain rows are reported separately and are never direct-deal candidates; chain completion remains outside Phase 1.
  </behavior>
  <action>Create failing classifier tests first, including a newer chain hold beside an otherwise matchable direct deal. Implement database-free named exports `stableHoldKey`, `stableReleaseKey`, `stableCreateKey`, `expectedEscrowStatus`, `validateEscrowInvariant`, and `classifyEscrowCandidateGraph`. Sort all IDs canonically, separate direct and chain graphs before matching, and never use timestamps or chat text as evidence. Preserve HIGH categories from RESEARCH: wrong/missing/multiple link, user, amount, kind, ref, status, orphan/dangling ref, duplicate key, balance/held/payout/counter anomaly.</action>
  <verify>
    <automated>node --test --test-name-pattern="classifier|chain exclusion" test/migration.integration.test.mjs</automated>
  </verify>
  <done>Classifier deterministically identifies only unique direct pairs and proves a direct flow cannot consume a chain hold.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement immutable snapshot, snapshot-only audit and restore verification</name>
  <files>scripts/audit-deal-escrow.mjs, scripts/backup-snapshot.mjs, scripts/verify-restored-copy.mjs, test/migration.integration.test.mjs, package.json</files>
  <behavior>
    - Backup refuses overwrite/path collision and emits a verified immutable snapshot manifest.
    - Audit refuses the resolved live path and any non-read-only open.
    - Restore verifier refuses the live path, checksum mismatch, integrity/FK failure or HIGH domain result.
    - Missing required CLI values or environment values fail closed before database access.
  </behavior>
  <action>Strengthen `backup-snapshot.mjs` around `VACUUM INTO`: explicit paths only, no overwrite, SHA-256/size/source/target/artifact/runtime/compile-options/PRAGMA evidence, then independently open the target for `integrity_check` and `foreign_key_check`. Implement `audit-deal-escrow.mjs` with its own Prisma client and read-only URI; never import `lib/prisma.js`. Its JSON and human output must derive from one canonical object and remain written even when HIGH findings cause nonzero exit. Implement `verify-restored-copy.mjs` with the same live-path denial and independent checks. Each Node entrypoint must validate its required CLI arguments or named `process.env.ESCROW_*` fallback, reject blank values, and have no `prisma/dev.db` or guessed-path default. Add serial `test`, `escrow:backup`, `escrow:audit`, and `escrow:verify-restore` scripts.</action>
  <verify>
    <automated>node --check scripts/audit-deal-escrow.mjs; node --check scripts/backup-snapshot.mjs; node --check scripts/verify-restored-copy.mjs; node --test --test-name-pattern="snapshot|read-only|missing input" test/migration.integration.test.mjs</automated>
  </verify>
  <done>The only supported production audit contract is live database → consistent immutable snapshot → read-only audit, with fail-closed inputs and evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI → SQLite path | Untrusted path selection must not turn an audit into a live write. |
| historical rows → classifier | Corrupt/ambiguous/chain rows cannot authorize direct backfill. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering / Repudiation | classification | mitigate | Global 1:1 graph, stable IDs/hash, chain exclusion and unchanged ambiguity report. |
| T-01-02 | Tampering | target selection | mitigate | Required resolved paths, live-path deny, mode=ro, no defaults, nonzero exit. |
| T-01-03 | Availability / Disclosure | snapshot evidence | mitigate | Unique target, checksum, integrity/FK/runtime evidence; policy ownership is gated in 01-03. |
</threat_model>

<verification>
Focused checks above complete in under 30 seconds. At the Wave 1 gate run `node --test --test-concurrency=1 test/migration.integration.test.mjs`. Audit source has no import of `lib/prisma.js`, and audit/restore/backup-target live-path denial is covered by test.
</verification>

<success_criteria>
- Immutable pre/post snapshots can be created and audited without auditing the live path.
- All HIGH buckets and chain exclusions are deterministic and machine-readable.
- Every missing or unsafe input stops before database access.
</success_criteria>

<output>
After completion create `.planning/phases/01-escrow-integrity-and-safe-migration/01-01-SUMMARY.md`.
</output>
