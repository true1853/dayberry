---
phase: 01-escrow-integrity-and-safe-migration
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/migration_lock.toml
  - prisma/migrations/20260814170000_baseline/migration.sql
  - prisma/migrations/20260814171000_deal_escrow_integrity/migration.sql
  - scripts/backfill-deal-escrow.mjs
  - test/fixtures/escrow-db.mjs
  - test/migration.integration.test.mjs
autonomous: true
requirements:
  - SAFE-02
must_haves:
  truths:
    - "Fresh and equivalent pre-change databases reach the same expanded nullable schema through reviewed Prisma migrations."
    - "Only globally unique invariant-consistent direct pairs enter a hash-guarded manifest; ambiguity remains unchanged."
    - "Rehearsal apply is non-live and idempotent; production apply is a separate fail-closed mode authorized only by a hash-bound one-time token."
    - "Chain rows remain compatible and untouched."
  artifacts:
    - path: prisma/migrations/20260814170000_baseline/migration.sql
      provides: exact reviewed pre-change baseline
    - path: prisma/migrations/20260814171000_deal_escrow_integrity/migration.sql
      provides: forward-only nullable relation and business keys
    - path: scripts/backfill-deal-escrow.mjs
      provides: dry-run default, non-live rehearsal apply and separately authorized production apply
    - path: test/fixtures/escrow-db.mjs
      provides: disposable fresh and pre-change SQLite fixtures
  key_links:
    - from: scripts/backfill-deal-escrow.mjs
      to: scripts/audit-deal-escrow.mjs
      via: audit checksum, canonical buckets, approved manifest hash and production authorization bundle
      pattern: "manifest|sha256|--rehearsal-apply|--production-apply"
    - from: prisma/schema.prisma
      to: prisma/migrations/20260814171000_deal_escrow_integrity/migration.sql
      via: generated and manually reviewed schema diff
      pattern: "escrowTransactionId|businessKey|DealEscrow"
---

<objective>
Create the reviewed additive migration and lossless, deterministic backfill required by SAFE-02.

Purpose: existing rows survive unchanged unless an immutable audit proves one exact direct-deal link.
Output: migration history, disposable fixtures, migration tests and backfill CLI.
</objective>

<execution_context>
@C:/Users/dev1853/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/dev1853/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-escrow-integrity-and-safe-migration/01-RESEARCH.md
@.planning/phases/01-escrow-integrity-and-safe-migration/01-VALIDATION.md
@.planning/phases/01-escrow-integrity-and-safe-migration/01-PATTERNS.md
@.planning/phases/01-escrow-integrity-and-safe-migration/01-01-SUMMARY.md
@prisma/schema.prisma
@scripts/migrate-chains.mjs

<interfaces>
Add nullable unique `Deal.createCommandKey`, nullable unique `Deal.escrowTransactionId`, relation `DealEscrow` with `onDelete: Restrict`, and nullable unique `Transaction.businessKey`. Retain `refType/refId`; do not add a generic unique constraint because chain rows share references.

Backfill is dry-run by default and has three mutually exclusive contracts. Dry-run and `--rehearsal-apply` require `--database`, `--live-path`, `--audit`, `--manifest`, and `--manifest-sha256`, always reject resolved database/live-path equality, and are the only modes used before 01-06. `--production-apply` is implemented here but may be invoked only by 01-06; it requires resolved `--database`, `--live-path`, and `--confirm-live-path` to be the same exact approved live path plus immutable pre-snapshot, audit, manifest, candidate, corrected-rollback and single-writer evidence hashes, a recomputable approval token, and an exclusive token-consumption ledger. Plain `--apply` is unsupported.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create and review baseline plus additive migration</name>
  <files>prisma/schema.prisma, prisma/migrations/migration_lock.toml, prisma/migrations/20260814170000_baseline/migration.sql, prisma/migrations/20260814171000_deal_escrow_integrity/migration.sql, test/fixtures/escrow-db.mjs, test/migration.integration.test.mjs</files>
  <behavior>
    - Baseline creates the exact old schema on a fresh DB and is resolved on an equivalent existing DB only after diff=empty proof.
    - Forward migration preserves IDs, rows, indexes and legacy fields while adding nullable FK/unique fields.
    - `prisma migrate deploy`, never `db push`, creates the expanded schema.
  </behavior>
  <action>Capture the exact pre-change baseline before editing the schema and add SQLite `migration_lock.toml`. Expand only with the contract fields. Generate the forward SQL and manually inspect every SQLite table-copy fragment for copy-back of all business columns/indexes/FKs, `onDelete Restrict`, and no destructive data loss. Build a unique OS-temp fixture with explicit `DATABASE_URL`, independent Prisma clients and teardown. Test fresh deploy and pre-change schema-equivalence → baseline resolve → deploy, preserving IDs/counts/values. Keep the explicit blocking gate: baseline resolve is forbidden unless schema diff is empty, and production uses `migrate deploy` only.</action>
  <verify>
    <automated>npx prisma validate</automated>
    <automated>node --test --test-name-pattern="baseline|migrate deploy|preserves" test/migration.integration.test.mjs</automated>
  </verify>
  <done>Reviewed migration history expands fresh and equivalent pre-change databases without loss.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement canonical dry-run and guarded exact-row backfill</name>
  <files>scripts/backfill-deal-escrow.mjs, test/fixtures/escrow-db.mjs, test/migration.integration.test.mjs</files>
  <behavior>
    - Dry-run emits exact deal/transaction/ref/business keys only for the canonical unique direct bucket.
    - Ambiguous/broken/chain rows are listed but never written.
    - Rehearsal apply rejects the live path, revalidates snapshot/audit/manifest hashes and every row inside a short transaction, and a second rehearsal apply produces `mutations=0`.
    - Production apply rejects every mode/path shortcut and requires exact live-path confirmation plus a hash-bound one-time approval token.
  </behavior>
  <action>Write failing happy, ambiguity, corruption, chain-collision, checksum/hash drift, rerun and production-authorization tests first. Implement dry-run default against a non-live snapshot/restored copy and emit canonical JSON plus SHA-256. `--rehearsal-apply` must reject resolved equality between `--database` and `--live-path`, verify audit source checksum/HIGH state and exact manifest hash, reread each row, and update only `Deal.escrowTransactionId`, direct `Transaction.refType/refId`, and `Transaction.businessKey`. Implement a separate `--production-apply` branch for 01-06 only: reject plain `--apply`; require nonblank named `process.env.ESCROW_*` values or matching explicit arguments; require `--database`, `--live-path`, and `--confirm-live-path` to resolve to the same approved live file; require immutable SHA-256 values for the pre-snapshot, pre-audit, manifest, actual candidate artifact, corrected-core rollback artifact, and single-writer evidence; recompute the approval token as SHA-256 of the canonical versioned authorization object containing those six hashes and the exact live path; and reserve that token with exclusive-create semantics in the evidence ledger before opening the live DB so reuse fails closed. Missing/mismatched evidence, token reuse, a rollback digest not marked and tested as corrected-core/expanded-schema compatible, HIGH findings, or row drift must stop before mutation. Do not populate legacy create keys or infer from time/text. Handle P2002 only after rereading the exact expected idempotent state. Production invocation belongs exclusively to non-autonomous plan 01-06; all automated tests use temporary databases.</action>
  <verify>
    <automated>node --test --test-name-pattern="dry-run|manifest|ambiguous|chain|production apply contract" test/migration.integration.test.mjs</automated>
  </verify>
  <done>SAFE-02 has a reproducible lossless migration and a fail-closed, idempotent direct-only backfill.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration SQL → existing SQLite | Table-copy SQL must preserve every business row and constraint. |
| reviewed audit → backfill | Only exact immutable-snapshot evidence authorizes row mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering / Repudiation | backfill | mitigate | Canonical manifest/hash, row reread, chain exclusion, unchanged ambiguity. |
| T-01-02 | Tampering | target path | mitigate | Dry-run/rehearsal require resolved non-live paths; production mode requires three-way exact live-path confirmation, six evidence hashes, recomputed one-time token and exclusive consumption receipt. |
| T-01-04 | Tampering / Denial | migration | mitigate | Schema-equivalence block, reviewed SQL, fresh/pre-change deploy tests; no db push. |
</threat_model>

<verification>
Run only the focused commands above per task. At the Wave 2 gate, run `npx prisma generate` followed by `node --test --test-concurrency=1 test/migration.integration.test.mjs`. Inspect SQL for data-copy completeness and preserve the migrate-deploy blocking gate. No production mutation occurs in this plan.
</verification>

<success_criteria>
- SAFE-02 appears only in this plan frontmatter.
- Fresh/pre-change migrations preserve data and constraints.
- Ambiguous and chain rows remain unchanged; rerun is a no-op.
</success_criteria>

<output>
After completion create `.planning/phases/01-escrow-integrity-and-safe-migration/01-02-SUMMARY.md`.
</output>
